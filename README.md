# Sanjeev Sadayappan Vinoth — Profile Gateway 🌐

This is the primary user domain configuration repository for `https://sanju-929.github.io`. 

The root `index.html` file acts as an automated routing hub. It reads the system `navigator.language` parameter from incoming browser requests to instantly guide users to the localized English or German variations of my core personal portfolio.
